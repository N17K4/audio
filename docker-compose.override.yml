# ローカル開発用（git にコミットしない）
# docker-compose up --build でローカルビルド＆起動
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
